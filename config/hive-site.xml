<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- ========================================= -->
  <!-- METASTORE DATABASE CONNECTION             -->
  <!-- ========================================= -->

  <property>
    <name>javax.jdo.option.ConnectionURL</name>
    <value>jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}</value>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionDriverName</name>
    <value>org.postgresql.Driver</value>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionUserName</name>
    <value>${POSTGRES_USER}</value>
  </property>

  <property>
    <name>javax.jdo.option.ConnectionPassword</name>
    <value>${POSTGRES_PASSWORD}</value>
  </property>

  <!-- ========================================= -->
  <!-- WAREHOUSE CONFIGURATION                   -->
  <!-- ========================================= -->

  <!--  <property>-->
  <!--    <name>hive.metastore.warehouse.dir</name>-->
  <!--    <value>${DELTALAKE_WAREHOUSE_DIR}</value>-->
  <!--  </property>-->

  <!-- ========================================= -->
  <!-- S3A CONFIGURATION FOR MINIO              -->
  <!-- ========================================= -->

  <property>
    <name>fs.s3a.endpoint</name>
    <value>${S3_ENDPOINT}</value>
  </property>

  <property>
    <name>fs.s3a.access.key</name>
    <value>${S3_ACCESS_KEY}</value>
  </property>

  <property>
    <name>fs.s3a.secret.key</name>
    <value>${S3_SECRET_KEY}</value>
  </property>

  <property>
    <name>fs.s3a.path.style.access</name>
    <value>true</value>
    <description>Use path-style access for S3A (required for MinIO)</description>
  </property>

  <property>
    <name>fs.s3a.connection.ssl.enabled</name>
    <value>false</value>
  </property>

  <property>
    <name>fs.s3a.impl</name>
    <value>org.apache.hadoop.fs.s3a.S3AFileSystem</value>
    <description>S3A filesystem implementation class</description>
  </property>

  <!-- ========================================= -->
  <!-- METASTORE SERVICE CONFIGURATION          -->
  <!-- ========================================= -->

  <property>
    <name>hive.metastore.client.capability.check</name>
    <value>false</value>
    <description>Disable client capability check to allow version mismatches</description>
  </property>

  <property>
    <name>hive.support.concurrency</name>
    <value>true</value>
  </property>

  <property>
    <name>hive.metastore.resolve.hostnames</name>
    <value>false</value>
    <description>Avoid canonicalizing URIs to Docker's network domain</description>
  </property>

  <!-- ========================================= -->
  <!-- SCHEMA MANAGEMENT                         -->
  <!-- ========================================= -->

  <property>
    <name>datanucleus.schema.autoCreateTables</name>
    <value>true</value>
    <description>Automatically create the necessary tables in the database schema if they don't exist</description>
  </property>


  <!-- ========================================= -->
<!-- REQUIRED FOR THIRD-PARTY S3 STORES       -->
<!-- ========================================= -->
<property>
  <name>fs.s3a.endpoint.region</name>
  <value>us-east-1</value>
  <description>Required for v4 signing - any non-empty value works for MinIO</description>
</property>

<property>
  <name>fs.s3a.change.detection.mode</name>
  <value>none</value>
  <description>Disable change detection to avoid HEAD requests that may hang</description>
</property>

<property>
  <name>fs.s3a.bucket.probe</name>
  <value>0</value>
  <description>Disable bucket probes at startup (default but explicit)</description>
</property>


<!-- ONLY use your configured credentials -->
<property>
  <name>fs.s3a.aws.credentials.provider</name>
  <value>org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider</value>
</property>

<!-- Disable AWS instance metadata -->
<property>
  <name>fs.s3a.iam.instance.credentials.enabled</name>
  <value>false</value>
</property>
<property>
  <name>fs.s3a.connection.timeout</name>
  <value>10000</value>
</property>
<property>
  <name>fs.s3a.socket.timeout</name>
  <value>10000</value>
</property>
<property>
  <name>fs.s3a.retry.limit</name>
  <value>2</value>
</property>



  <property>
  <name>fs.s3a.s3.signing-algorithm</name>
  <value>AWS4SignerType</value>
  <description>Force S3A to use AWS Signature V4 for S3 requests</description>
</property>


</configuration>